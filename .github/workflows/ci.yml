name: CI — Lint, Test, Build

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

env:
  GO_VERSION: "1.23"
  PYTHON_VERSION: "3.12"
  NODE_VERSION: "20"

jobs:
  hello:
    name: Smoke test
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI is alive"
